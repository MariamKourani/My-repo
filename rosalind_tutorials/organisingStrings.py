#!/usr/bin/python
import itertools
from itertools import permutations

s = 'A C T G';
#sSorted = ''.join(sorted(s));
#print (sSorted);

String_list = [] #empty list
for i in range (0, len(sSorted)):
    print(String_list.append(sSorted[i:len(sSorted)])
#i=0;
#list =[];
#length = len(sSorted);
#permList = list(range(1, length+1));


#for k in sSorted:
    #String_list.append(k)
#print(String_list)


#list = sSorted.split();
#print(list);
# print the permutation lists:
#perm = itertools.permutations(list);
#print(perm);
# #print(permutations(range(5),2));
